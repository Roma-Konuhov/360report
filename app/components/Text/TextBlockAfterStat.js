import React from 'react';

class TextBlockAfterStat extends React.Component {
  render() {
    return (
      <p className="text-block">Comments compiled in this section are recorded exactly as entered by the respondents - they are not edited, emphasized, ordered or filtered in any way. Where comments appear to be duplicated it is where the same comment has been entered by more than one respondent.</p>
    );
  }
}

export default TextBlockAfterStat;